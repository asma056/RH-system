import styled from 'styled-components'

export const Container = styled.header`
  position: absolute;
  top: 0;
  z-index: 3;
  margin: 0;
  width: 100vw;
  display: flex;
  flex-direction: row;
  justify-content: flex-end;
  align-items: flex-end;
  background-color: var(--white);
  box-shadow: 0px 1px 8px rgba(139, 139, 139, 0.5);
`
